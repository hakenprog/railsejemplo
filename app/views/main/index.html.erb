<p id="notice"><%= notice %></p>

<h1>Página principal</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clients.each do |client| %>
      <tr>
        <td><%= client.nombre %></td>
        <td><%= link_to 'ver gastos', main_show_path_url(client.id) %></td>
       </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'clients', clients_path %>
<%= link_to 'services', services_path %>
